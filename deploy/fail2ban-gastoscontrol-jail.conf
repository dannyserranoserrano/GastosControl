# GastoControl — jail fail2ban (bloquea IPs que abusan del OCR)
# Instala en /etc/fail2ban/jail.d/gastoscontrol.conf
[gastoscontrol-ocr]
enabled  = true
filter   = gastoscontrol-ocr
logpath  = /var/log/apache2/gastoscontrol-access.log
maxretry = 15
findtime = 300
bantime  = 3600
